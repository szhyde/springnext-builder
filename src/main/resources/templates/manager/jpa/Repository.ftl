package ${projectInfo.packageName}.${projectInfo.modelName}.repository.jpa;

import ${projectInfo.packageName}.base.repository.jpa.BaseDao;
import ${projectInfo.packageName}.${projectInfo.modelName}.entity.${po.type.typeName};

/**
 * ${po.remark}DAO
 * @author ${projectInfo.author}
 *
 */
public interface ${po.type.typeName}Dao extends BaseDao<${po.type.typeName}, String> {
	
}
